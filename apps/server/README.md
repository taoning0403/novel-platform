# Novel Platform Server

The FastAPI application for the `novel-platform` v0.8.0 modular monolith. v0.8.0 hardens the
trusted proxy and authentication flows while preserving the accepted API, permissions, and
persistent model. See the repository README for setup, authentication, migration, generated-client,
and verification commands.
